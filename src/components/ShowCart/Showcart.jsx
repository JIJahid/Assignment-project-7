import PropTypes from 'prop-types'

const Showcart = ({showcar}}) => {
    
    return (
        <div>
            
        </div>
    );
};

Showcart.propTypes = {
    showcart: PropTypes.object
}

export default Showcart;